0.2.6
    added test for 'delay=True'

    added (as yet untested) code:
        LockingRotatingFileHandler (locking_handlers.py),
        add_rotating_file_handler (logging_config_dict_ex.py)

0.2.5
    use (possibly 'ext://'-prefixed) strings for streams and callables,
    in case of clone_handler(), which uses deepcopy --
    LockingStreamHandler, sys.std*** are not deepcopy-able/serializable.

0.2.4
    Made creation of console handlers separate methods,
    simplified __init__ signature & implementation

0.2.3
    Tweak to emit/flush in locking_handlers.py

0.2.2
    adding Ex's handlers to root logger, see if that breaks anything,
    and if it allows simplification e.g. in client code.
    trapping add_handler, adding flag ``add_to_root=True``.
    You can also pass ``add_to_root`` to add_file_handler, it will end up
    in **kwargs, passed to add_handler.

0.2.1 =
0.1.3
    cleanup
0.1.2
    LoggingConfigDictEx
0.1.1
    added LockingStreamHandler, LockingFileHandler
0.1.0
    setup_logging, first version, non-mp-aware
